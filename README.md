# sysglance
A lightweight CLI tool for real-time CPU and system metrics. Parsing via the Linux /proc filesystem

# state
As of today (june, 15th, 2026) the project is started (Repo creation).
Any updates or final commits will be published here - to let you have a glance (pun intended) at the current state of sysglance


